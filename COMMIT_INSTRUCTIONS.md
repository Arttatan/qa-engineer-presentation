# 📝 Подробная инструкция: Как создать коммит

## Что такое коммит?
**Коммит** — это сохранение изменений в истории Git. Это как "снимок" вашего проекта в определенный момент времени.

---

## 🎯 Шаг 1: Откройте терминал (PowerShell или Command Prompt)

### Вариант A: Через Cursor/VS Code
1. В Cursor нажмите **Ctrl + `** (обратная кавычка) или
2. Меню: **View → Terminal**

### Вариант B: Через Windows
1. Нажмите **Win + R**
2. Введите `powershell` или `cmd`
3. Нажмите **Enter**

---

## 🎯 Шаг 2: Перейдите в папку проекта

В терминале введите (скопируйте и вставьте):

```powershell
cd c:\Users\35191\Presentation
```

**Что делает эта команда?**
- `cd` = "change directory" (сменить директорию)
- Переводит вас в папку с вашим проектом

**Проверка:** После выполнения команды вы должны увидеть путь в начале строки терминала:
```
PS C:\Users\35191\Presentation>
```

---

## 🎯 Шаг 3: Проверьте статус Git

Введите команду:

```powershell
git status
```

**Что вы увидите:**
- Список файлов, которые готовы к коммиту (зеленым или под заголовком "Changes to be committed")
- Это нормально! Файлы уже добавлены в staging area

**Пример вывода:**
```
On branch main
Changes to be committed:
  (use "git reset HEAD <file>..." to unstage)
        new file:   .gitignore
        new file:   GITHUB_SETUP.md
        new file:   index.html
        new file:   README.md
        new file:   script.js
        new file:   styles.css
```

---

## 🎯 Шаг 4: Настройте Git (если еще не настроено)

**Проверьте, настроен ли Git:**
```powershell
git config user.name
git config user.email
```

**Если команды ничего не вывели или выдали ошибку**, настройте Git:

### Вариант A: Только для этого проекта (рекомендуется)
```powershell
git config user.name "Artem"
git config user.email "ваш.email@example.com"
```
*(Замените на ваше имя и email)*

### Вариант B: Для всех проектов на компьютере
```powershell
git config --global user.name "Artem"
git config --global user.email "ваш.email@example.com"
```

**Что делают эти команды?**
- Сохраняют ваше имя и email для Git
- Это нужно, чтобы Git знал, кто делает коммиты

---

## 🎯 Шаг 5: Создайте коммит

Введите команду:

```powershell
git commit -m "Initial commit: QA Engineer presentation website"
```

**Что делает эта команда?**
- `git commit` — создает коммит (сохранение)
- `-m` — флаг для сообщения коммита
- `"Initial commit: QA Engineer presentation website"` — описание того, что вы сохраняете

**Что вы увидите после успешного выполнения:**
```
[main (root-commit) abc1234] Initial commit: QA Engineer presentation website
 6 files changed, 1234 insertions(+)
 create mode 100644 .gitignore
 create mode 100644 GITHUB_SETUP.md
 create mode 100644 index.html
 create mode 100644 README.md
 create mode 100644 script.js
 create mode 100644 styles.css
```

**Это значит:** ✅ Коммит успешно создан!

---

## 🎯 Шаг 6: Проверьте, что коммит создан

Выполните команду:

```powershell
git log
```

**Что вы увидите:**
- Список коммитов (пока один)
- Ваше имя, email, дату и сообщение коммита

**Чтобы выйти из просмотра лога:**
- Нажмите клавишу **Q** (quit)

---

## ✅ Готово! Коммит создан

Теперь ваши файлы сохранены в истории Git. Следующий шаг — отправить их на GitHub.

---

## 🔍 Возможные проблемы и решения

### Проблема 1: "Author identity unknown"
**Ошибка:**
```
Author identity unknown
*** Please tell me who you are.
```

**Решение:**
Выполните Шаг 4 (настройка Git) и повторите коммит.

---

### Проблема 2: "nothing to commit"
**Ошибка:**
```
nothing to commit, working tree clean
```

**Решение:**
Файлы уже закоммичены или не добавлены. Проверьте:
```powershell
git status
```

Если файлы не добавлены:
```powershell
git add .
git commit -m "Initial commit: QA Engineer presentation website"
```

---

### Проблема 3: Команда не найдена
**Ошибка:**
```
'git' is not recognized as an internal or external command
```

**Решение:**
Git не установлен. Скачайте и установите с [git-scm.com](https://git-scm.com/download/win)

---

## 📋 Краткая шпаргалка команд

```powershell
# 1. Перейти в папку проекта
cd c:\Users\35191\Presentation

# 2. Проверить статус
git status

# 3. Настроить Git (если нужно)
git config user.name "Artem"
git config user.email "ваш.email@example.com"

# 4. Создать коммит
git commit -m "Initial commit: QA Engineer presentation website"

# 5. Проверить результат
git log
```

---

## 🎓 Что дальше?

После создания коммита переходите к отправке на GitHub:
1. Создайте репозиторий на GitHub.com
2. Подключите локальный репозиторий к GitHub
3. Отправьте код командой `git push`

Подробная инструкция в файле `GITHUB_SETUP.md`

---

**Нужна помощь?** Если что-то не работает, скопируйте текст ошибки и сообщите мне!