# 🚀 Как залить сайт на GitHub через Cursor

## 📋 Пошаговая инструкция

### Шаг 1: Откройте панель Source Control

1. В Cursor нажмите на иконку **ветки Git** в левой панели (или нажмите **Ctrl + Shift + G**)
2. Вы увидите панель "Source Control" с вашими файлами

---

### Шаг 2: Проверьте файлы для коммита

В панели Source Control вы увидите:
- **Changes** — файлы, готовые к коммиту (зеленым)
- **Untracked** — новые файлы, которые еще не добавлены

**Если есть файлы в "Untracked":**
- Нажмите на иконку **"+"** рядом с файлом или
- Нажмите правой кнопкой мыши на файл → **"Stage Changes"**

---

### Шаг 3: Создайте коммит

1. В верхней части панели Source Control есть поле **"Message"** (Сообщение коммита)
2. Введите сообщение, например:
   ```
   Initial commit: QA Engineer presentation website
   ```
3. Нажмите кнопку **"✓ Commit"** (галочка) или **Ctrl + Enter**

**Что вы увидите:**
- Файлы исчезнут из списка "Changes"
- Внизу появится сообщение "Successfully committed"

---

### Шаг 4: Настройте Git (если еще не настроено)

Если при коммите появится ошибка про имя/email:

1. Откройте терминал в Cursor: **Ctrl + `**
2. Выполните команды:
   ```powershell
   git config user.name "Artem"
   git config user.email "ваш.email@example.com"
   ```
3. Повторите Шаг 3 (создание коммита)

---

### Шаг 5: Создайте репозиторий на GitHub

1. Откройте браузер и перейдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните:
   - **Repository name**: `qa-engineer-presentation` (или любое другое имя)
   - **Description**: "Современный одностраничный сайт-презентация QA Engineer"
   - Выберите **Public** или **Private**
   - ⚠️ **ВАЖНО:** НЕ ставьте галочки на:
     - ❌ "Add a README file"
     - ❌ "Add .gitignore"
     - ❌ "Choose a license"
   - (У нас уже есть эти файлы!)
6. Нажмите **"Create repository"**

---

### Шаг 6: Подключите репозиторий к GitHub

После создания репозитория GitHub покажет страницу с инструкциями. 

**В Cursor:**

1. Откройте терминал: **Ctrl + `**
2. Убедитесь, что вы в правильной папке:
   ```powershell
   cd c:\Users\35191\Presentation
   ```
3. Скопируйте URL вашего репозитория с GitHub (он будет вида: `https://github.com/YOUR_USERNAME/qa-engineer-presentation.git`)
4. Выполните команды (замените URL на свой):

```powershell
git remote add origin https://github.com/YOUR_USERNAME/qa-engineer-presentation.git
git branch -M main
git push -u origin main
```

**Или через интерфейс Cursor:**

1. В панели Source Control нажмите на **"..."** (три точки) вверху
2. Выберите **"Remote" → "Add Remote"**
3. Введите:
   - **Name**: `origin`
   - **URL**: `https://github.com/YOUR_USERNAME/qa-engineer-presentation.git`
4. Нажмите **"Add Remote"**
5. Затем нажмите **"..."** → **"Push"** → **"Push to..."** → выберите `origin/main`

---

### Шаг 7: Отправьте код на GitHub

**Вариант A: Через терминал (проще)**

После выполнения команд из Шага 6 код автоматически отправится на GitHub.

**Вариант B: Через интерфейс Cursor**

1. В панели Source Control нажмите **"..."** (три точки)
2. Выберите **"Push"** → **"Push to..."**
3. Выберите `origin/main` (или `origin/master`)
4. Если появится запрос на аутентификацию:
   - Введите ваш GitHub username
   - Введите **Personal Access Token** (не пароль!)
   - Как создать токен: см. ниже

---

### 🔐 Создание Personal Access Token (если нужно)

GitHub больше не принимает пароли. Нужен токен:

1. На GitHub: **Settings** (ваш профиль) → **Developer settings**
2. **Personal access tokens** → **Tokens (classic)**
3. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
4. Заполните:
   - **Note**: "Cursor Git Access"
   - **Expiration**: выберите срок (например, 90 дней)
   - **Scopes**: отметьте **`repo`** (полный доступ к репозиториям)
5. Нажмите **"Generate token"**
6. **Скопируйте токен сразу!** (он больше не будет показан)
7. Используйте этот токен вместо пароля при push

---

### ✅ Проверка результата

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все ваши файлы:
   - index.html
   - styles.css
   - script.js
   - README.md
   - и другие

**Готово! Ваш сайт на GitHub! 🎉**

---

## 🎨 Включение GitHub Pages (чтобы сайт был доступен по ссылке)

1. На странице репозитория на GitHub нажмите **"Settings"**
2. В меню слева выберите **"Pages"**
3. В разделе **"Source"**:
   - **Branch**: выберите `main` (или `master`)
   - **Folder**: выберите `/ (root)`
4. Нажмите **"Save"**
5. Через 1-2 минуты ваш сайт будет доступен по адресу:
   ```
   https://YOUR_USERNAME.github.io/qa-engineer-presentation/
   ```

---

## 🔄 Обновление сайта в будущем

После изменений в файлах:

1. Откройте панель Source Control (**Ctrl + Shift + G**)
2. Добавьте измененные файлы (иконка "+")
3. Введите сообщение коммита
4. Нажмите **"✓ Commit"**
5. Нажмите **"..."** → **"Push"** → **"Push to..."** → `origin/main`

Или через терминал:
```powershell
git add .
git commit -m "Описание изменений"
git push
```

---

## ❓ Частые проблемы

### Проблема: "Authentication failed"
**Решение:** Используйте Personal Access Token вместо пароля (см. выше)

### Проблема: "Repository not found"
**Решение:** Проверьте, что:
- Репозиторий создан на GitHub
- URL правильный (скопируйте с GitHub)
- Вы авторизованы в GitHub

### Проблема: "Branch 'main' does not exist"
**Решение:** Выполните:
```powershell
git branch -M main
```

### Проблема: "No upstream branch"
**Решение:** При первом push используйте:
```powershell
git push -u origin main
```

---

## 📸 Визуальная подсказка

В Cursor панель Source Control выглядит так:

```
┌─────────────────────────────┐
│ SOURCE CONTROL              │
├─────────────────────────────┤
│ Message: [________________] │ ← Введите сообщение здесь
│         [✓ Commit]          │ ← Нажмите для коммита
├─────────────────────────────┤
│ CHANGES                      │
│  ✓ index.html               │
│  ✓ styles.css               │
│  ✓ script.js                │
│                              │
│ UNTRACKED                   │
│  + new-file.md              │ ← Нажмите + для добавления
└─────────────────────────────┘
```

---

**Удачи! Если что-то не работает, напишите мне! 🚀**